# Copy your InsForge-provisioned OpenRouter key from the InsForge dashboard
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini
# On GCP VM pipeline-demo:
KUBECONFIG_PATH=/home/kunudharme/.kube/config
# Docker on GKE VM uses /kube/config (see docker-compose.gke.yml)
# GKE_VALIDATE_ON_STARTUP=true is set in docker-compose.gke.yml

# Windows dev: run kubectl on the VM via SSH when local kubeconfig is missing
KUBECTL_SSH_HOST=kunudharme@34.47.189.50
KUBECTL_SSH_KUBECONFIG=/home/kunudharme/.kube/config

# InsForge (realtime progress publishing)
INSFORGE_BASE_URL=https://buhwez72.ap-southeast.insforge.app
INSFORGE_ANON_KEY=